import { PageShell } from "@/components/dashboard/page-shell";

export default function ProfileManagerPage() {
    return (
        <PageShell
            title="Profile Manager"
            description="Manage your admin profile and Google Business Profile connection."
        />
    );
}
