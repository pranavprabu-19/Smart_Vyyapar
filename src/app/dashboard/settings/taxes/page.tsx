import { PageShell } from "@/components/dashboard/page-shell";

export default function TaxSettingsPage() {
    return (
        <PageShell
            title="TCS / TDS Rates"
            description="Configure tax rates for Tax Collected at Source and Tax Deducted at Source."
        />
    );
}
